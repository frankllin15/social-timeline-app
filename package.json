{
  "name": "grupo-morada-teste",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "start": "concurrently -c \"green,cyan\" \"cd frontend && npm run build && npm run preview\" \"cd backend && npm run build && npm start\"",
    "install": "concurrently \"cd frontend && npm i -f --silent\" \"cd backend && npm i\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.2"
  }
}
